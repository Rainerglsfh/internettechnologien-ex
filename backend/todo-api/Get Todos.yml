info:
  name: Get Todos
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/todos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
